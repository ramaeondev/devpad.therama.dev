# E2E environment example
# URL the E2E runner will use. Default in playwright.config.ts is http://localhost:4200
E2E_BASE_URL=http://localhost:4200

# If you want to run a subset of tests against a real Supabase test project, set these secrets in CI
# SUPABASE_URL=https://your-test-supabase-url
# SUPABASE_KEY=your-service-role-or-test-key
# E2E_ADMIN_KEY=optional-admin-key-for-setup
