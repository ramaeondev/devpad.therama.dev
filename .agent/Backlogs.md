# Backlogs

- Chunked streaming encryption/decryption for large files (AES-GCM per-chunk with unique IVs, authenticated chunk metadata, and reassembly). Not needed now due to 5MB file limit, but plan for scalability and memory efficiency.
